import HeaderMenu from "./HeaderMenu";

function Header() { 
    return (
        <header>
            <nav>
                <HeaderMenu />
            </nav>
        </header>
    )
};
export default Header;